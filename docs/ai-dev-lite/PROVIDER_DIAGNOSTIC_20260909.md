# duoyuanx 双参考图受控诊断（2026-09-09）

## 目的与边界

- FACT：针对线上任务 `generation_4aa90740-2322-4b8b-9ea9-ee2c6acebf29` 的 `PROVIDER_TIMEOUT`，比较仅原图、双参考图以及降低输出尺寸时的上游响应时间。
- 使用该任务已存储的原房间图和项目内侘寂风卡片图，不输出图片内容、API Key、完整响应或请求头。
- 诊断直接调用当前启用的 duoyuanx Provider，不经过网站生成服务，不创建任务或作品，也不修改网站额度。
- 每项请求最多等待 120 秒。HTTP 200 项确认响应 JSON 包含图片 URL 字段；本轮未保存或展示生成图片，不将其视为视觉质量验收。
- 第一次诊断进程的临时 systemd 输出未被持久化，可能已经向 Provider 发起最多 3 次额外请求；其结果未用于结论。随后重新执行的 3 次请求均有脱敏持久化证据。Provider 可能按所有实际接收的请求计费。

## 脱敏结果

| 项目 | 参考图 | 输出尺寸 | 结果 | Provider 耗时 | Trace ID |
| --- | ---: | ---: | --- | ---: | --- |
| 仅原图 | 1 | 1248×832 | HTTP 200，响应含图片 URL | 91,958 ms | `provider_diag_one-reference_2d0e42b4-6214-4266-b3ab-0f2f5593569d` |
| 原图 + 侘寂风参考图 | 2 | 1248×832 | 120 秒超时，未收到 HTTP 响应 | 120,004 ms | `provider_diag_two-references_d76b1b10-b2dc-4a5b-a979-7ef3d4546dd5` |
| 原图 + 侘寂风参考图（较小尺寸） | 2 | 1024×688 | HTTP 200，响应含图片 URL | 61,805 ms | `provider_diag_two-references-small_96771aef-a855-477f-bbef-bc5edf727f6c` |

## 结论

- FACT：当前网站上游请求超时是 90 秒；仅原图标准尺寸本次也需要约 91.96 秒，因此 90 秒阈值偏短。
- FACT：双参考图标准尺寸超过 120 秒；相同双参考图降为 1024×688 后约 61.81 秒返回。双参考图协议可返回结果，但处理速度受输出像素或上游渠道负载显著影响。
- FACT：1024×688 保留约 3:2 比例、宽高均为 16 的倍数、总像素 704,512，符合供应商公布的自定义尺寸范围，且略高于本次 960×640 输入图的像素数。
- DECISION（用户于 2026-09-09 批准）：duoyuanx 双参考图将目标像素控制在约 70 万并保持原图比例；上游超时提高到 150 秒、生成服务外层兜底设为 180 秒。降低目标像素用于提速，提高超时仅用于覆盖偶发排队。
- 风险：单次实测不能证明所有请求稳定；中转站渠道负载仍可能波动。部署修复后应连续做少量受控真实生成，并分别记录 Provider、COS 和非 Provider 耗时。

## 无副作用证据

- `diagnosticGenerationTasks: 0`
- `diagnosticWorks: 0`
- `diagnosticCreditOperations: 0`
- API 服务：`active`
- 健康检查：HTTP 200

## 本地修复验证

- `npm test`：105/105 通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- 自动化覆盖：duoyuanx 双参考图的 3:2 输入请求 `1024x688`；单参考图继续使用原像素预算；Provider 超时仍不重试；生成失败仍释放预留额度，成功仍只结算 1 点。
